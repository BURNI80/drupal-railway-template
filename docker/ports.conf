# Railway requires IPv4 for the edge proxy to reach the container.
# Debian's default ports.conf binds Apache to [::]:80 which can resolve to
# IPv6-only, so the Railway proxy gets "connection refused". Force an explicit
# IPv4 listener on port 80 (the only port Railway routes to).
Listen 0.0.0.0:80